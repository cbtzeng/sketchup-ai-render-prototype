# open-questions.md — 待你回覆的問題

**填寫方式**：直接在每題的 `**答：**` 後面寫。留白代表「用我的預設建議」。
你回完之後我才會進下一步（寫 implementation plan）。

分成三類：

- **A. 決策題** —— 只有你能決定，會改變架構或範圍。
- **B. 實機驗證題** —— 需要你在裝了 SketchUp 的機器上跑一下才知道答案。我沒有 SketchUp，猜不得。
- **C. 背景題** —— 影響優先序，不影響架構。

---

## A. 決策題

### Q1. 目標 SketchUp 版本與作業系統？ ✅ 已自動確認

掃描結果：**SketchUp 2026 (26.2.242)、Ruby 3.2.2、macOS 15.7.1、universal binary**。
除非你要同時支援舊版或 Windows，這題不用回答，就鎖 2026 / macOS。

**答：**（若需支援其他版本才填）

---

### Q2. 這份作業的時間盒有多長？（幾天 / 幾小時）

這直接決定 MVP 要不要再砍。

- 若 ≤ 2 天：評估縮到 6 場景 × 2 相機 = 12 shots，只做 Edge F-score 一個指標。
- 若 ≥ 5 天：照 scope.md 的 MVP 全做，並加語意遮罩 pass。

**答：先以 ≤ 2 天來做 後續再往晚整 scope 執行**

> **我的回覆（已解衝突）**：你原本 Q3 選 (a)+(b)，跟 ≤2 天做不完 —— RBZ + demo 影片要求穩定性，
> 評估報告要求跑批與統計，兩天只夠做好其中一邊。你後來改成只要 (b)，衝突解除。
> **2 天的分配**：Day 0 上午做 fog 標定與對齊驗證（決定架構）→ Day 0 下午到 Day 1 上午做擷取與雲端最小路徑
> → Day 1 下午跑 12 shots 評估並寫報告。文件已寫完，不佔時間。

---

### Q3. 交付物形式是什麼？（面試方期待看到什麼）

- (a) 可安裝執行的 RBZ + demo 影片
- (b) 程式碼 repo + 設計文件 + 評估報告
- (c) 只有文件與規劃
- (d) 現場口頭簡報 + 投影片

不同答案會讓我把力氣放在完全不同的地方。若是 (b)，評估報告比功能重要；若是 (a)，穩定性比評估重要。

**答：(a) + (b)**

> **已更新為 (b)**（使用者於對話中修正）。影響：
> - 不做 RBZ 打包與 demo 影片，省下的時間全部投入評估報告。
> - 外掛只要能在你機器上跑通完整流程即可，不需要處理安裝、簽章、他人環境相容。
> - **評估報告成為最重要的交付物**，`eval/report.md` 的品質決定這份作業的成敗。

---

### Q4. 金鑰模型：平台 key + 額度，還是 BYO key？

見 critique.md 第 6 點——你原本的描述兩者混用了，需要選一個。

- 建議：**平台 key + 固定測試額度**。理由：成本護欄、去重快取、冪等計費在 BYO key 下都失去意義，
  而且保管別人的 API key 是不必要的責任。BYO key 列為文件中的未來選項。

**答：平台 key + 固定測試額度**

---

### Q5. 雲端選 Vercel 還是 Cloudflare Workers？

- 建議：**Vercel**。理由：Node 生態、Supabase SDK 支援直接、webhook 與長一點的 function 執行時間較好處理，
  而這個場景不需要 Workers 的邊緣延遲優勢（瓶頸在 GPU 推論，不在網路 hop）。
- 若你已經有 Cloudflare 的既有基礎建設，就選 Workers，我照改。

**答：Vercel**

---

### Q6. fal.ai 和 Replicate，你有帳號與額度嗎？兩家都能測嗎？

architecture.md 第 6 節的比較表目前是**我的預期，不是已驗證事實**。

- 兩家都能測 → MVP 就接一家、加分項補第二家，報告裡放實測 p50/p95 與單張成本。
- 只有一家 → 直接鎖定，比較表改成「文獻/定價頁調查」並註明未實測。

**答：如果都不接可以嗎？有接跟沒接的影響為何？**（有哪家？有無預算上限？）

> **我的回覆：不接的話這份作業就不成立。**
>
> 你的核心論點是「多重控制圖能提高結構保真度」。這是一個**關於生成結果的主張** ——
> 不生成任何圖，就沒有任何證據，H1/H2 都無法檢驗。剩下的只有一份漂亮的規劃文件，
> 也就是 Q3 的 (c)，而你已經選了 (b)。
>
> **有接 vs 沒接：**
>
> | | 有接（至少一家） | 都不接 |
> |---|---|---|
> | 可交付 | 規劃 + 可跑的外掛 + **評估報告** | 只有規劃文件 |
> | H1 能否檢驗 | 可以 | 不行 |
> | 對面試的說服力 | 「我量過，數據在這」 | 「我認為會比較好」 |
> | 風險 | 花小錢 | 論點無法證明 |
>
> **成本其實非常小。** 12 shots × 3 條件 × 1 seed = 36 張，
> 以每張 US$0.03–0.05 估，**約 US$1–2**；把重跑與試錯算進去抓 **US$10 上限**綽綽有餘。
> 這是一頓午餐的錢，換整份作業唯一的實證基礎。
>
> **建議**：接 **fal.ai** 一家就好（延遲較低、適合互動式外掛的體感）。
> Replicate 留給加分項；若沒時間，`architecture.md` 第 6 節的比較表就改成
> 「依定價頁與文件調查，未實測」並明講 —— 這比假裝測過誠實。
>
> **若你堅持都不接**，我會把 scope 改成純規劃 + 一份「評估協議（protocol）」，
> 說明如果有 key 會怎麼跑。這是次佳解，但要有心理準備：面試官會問「所以結果呢」。

---

### Q7. 是否採納 critique.md 第 1 點的建議，把「材質/物件語意遮罩」加入論點？

這是我認為最能強化你論點的一項，但會增加範圍（需要遍歷 model 的 material / group 並產生分色圖）。

- (a) 加進 MVP（論點最強，但要多花時間）
- (b) 留在加分項，先在文件中論述（建議，風險低）
- (c) 不做，論點維持 edge + depth

**答：(b)**

---

### Q8. 評估報告若顯示「C 打不贏 B（外部 Canny）」，你要怎麼呈現？

- (a) 誠實寫進報告，並把論點改寫成「護城河在語意與相機真值」（建議，我認為這反而加分）
- (b) 調整實驗設定重跑直到有優勢
- (c) 其他

我需要先知道你的立場，因為這會影響我怎麼寫評估的結論章節。

**答：(a)**

---

## B. 實機驗證題（需要你在 SketchUp Ruby Console 跑）

> 這一節對應 `sketchup-api-feasibility.md` 第 7 節。
> 我沒有 SketchUp 可測，這些我不會猜。答案回填後，🔴 項目才能降級。

### Q9. 環境基本資料 ✅ 大致已確認

已由檔案系統取得（見 Q1）。只剩一件事要你在 Ruby Console 確認：
`RUBY_VERSION` 實際回傳值是否為 `3.2.2`。

**答：**

---

### Q10. `model.rendering_options` 的完整 key 列表與當前值

請 dump 出來整份貼回。之後所有 key 名一律以這份為準——我不會憑印象寫 key。

**答：先告訴我怎麼取得**

```
（貼在這裡）
```

> **我的回覆**：已寫成 skill，見 `.claude/skills/sketchup-api-verify/SKILL.md` 的 **V2** 一節。
> 摘要（在 SketchUp 的 Ruby Console 貼上）：
>
> ```ruby
> ro = Sketchup.active_model.rendering_options
> puts "可用方法：#{(ro.methods - Object.instance_methods).sort.inspect}"
> if ro.respond_to?(:each_pair)
>   ro.each_pair { |k, v| puts "#{k}\t#{v.inspect}" }
> elsif ro.respond_to?(:each_key)
>   ro.each_key { |k| puts "#{k}\t#{ro[k].inspect}" }
> end
> ```
>
> 注意第一行：**先問它支援哪些方法，不要假設 `each_pair` 一定存在。**
> 這就是「不臆造 API」的實際做法。輸出太長不好貼的話，skill 裡有存檔版本。

---

### Q11. write_image 的寬高比行為（最高風險項）

viewport 維持一般比例（例如 16:9），呼叫 `write_image` 輸出 1024×1024，
觀察結果是：被裁切 / 上下加邊 / 重新取景（看到更多東西）？
接著設定 `camera.aspect_ratio` 後重測，行為有無改變？

**答：先做上下加邊 **

> **我的回覆：這一題不是設計選擇，是要量出來的事實。**
>
> `write_image` 在寬高比不符時做什麼，是 SketchUp 已經寫死的行為 ——
> 我們「決定要上下加邊」不會讓它就真的加邊。如果它其實是重新取景（看到更多東西），
> 三張控制圖的視野就不一致，ControlNet 會收到互相矛盾的條件，
> **結果可能比純 img2img 更差**。這個方案沒有「效果普通」的中間地帶。
>
> **但你的直覺指向對的解法**：與其去適應它的行為，不如**明確設定
> `camera.aspect_ratio` 並用相符的輸出比例**，讓寬高比不符這件事根本不發生。
> 這樣三個 pass 必然共用同一組相機參數。
>
> 驗證腳本見 skill 的 **V3**：跑 A（不動 camera）和 B（鎖 aspect_ratio）兩張，
> 比對後回報。這是 Day 0 上午第一件事。

---

### Q12. fog 深度標定（決定成敗）

建一個階梯測試模型：相機正前方 1 / 2 / 5 / 10 / 20 / 50 公尺各放一面白色垂直牆面。
開 fog、fog 色設純黑、關邊線、面設白色，`write_image` 後讀出每面牆中心的灰階值。
請回報：

- (a) 灰階 ↔ 距離的對照表（6 組數字）
- (b) `FogStartDist` / `FogEndDist` 設什麼值
- (c) 背景/天空區域是什麼顏色
- (d) 灰階變化看起來像線性還是指數

**答：你給我建議**

> **我的回覆**：完整程序見 skill 的 **V4**。建議起始設定：
>
> | 設定 | 建議值 | 理由 |
> |---|---|---|
> | 霧色 | 純黑 | 遠 = 暗，灰階單調遞增 |
> | 用背景色當霧色 | 關 | 否則量不到霧色 |
> | FogStartDist | 0 | 斜率從相機起算，好擬合 |
> | FogEndDist | 60（略大於最遠牆 50 m） | 若 50 m 已全黑，代表單位不是公尺或非線性 |
> | 邊線 / 貼圖 / 陰影 | 全關，面設純白 | 只留深度訊號 |
>
> **關鍵補充**：除了 6 組灰階，請多做一次「把 End 從 60 改成 30」再量一遍。
> 兩組數字放在一起，才看得出單位到底是什麼 —— 只量一次是看不出來的。
>
> 判讀：接近直線 → 可用，只需做 z→視差轉換；很快飽和 → 指數，要擬合查表；
> 不單調 → **fog 不能當深度**，改用 V5 的 raytest 當評估量尺，depth pass 退出 MVP。

---

### Q13. 副作用檢查

改 rendering_options 之後：

- `Sketchup.active_model.modified?` 變成 true 嗎？
- 按 Ctrl+Z 會撤銷到我們改的設定嗎？
- 還原後再 dump 一次，跟 Q10 的原始值逐項比對，有無不一致（特別是浮點數）？

**答：1. 是, 2. 是，要撤銷, 3. yes**

> **我的回覆：這三個答案我判斷是你的推測而非實測**（因為 Q10 你還在問怎麼 dump，
> 而第 3 項需要先有 dump 才能比對）。如果我判斷錯了、你真的跑過了，跟我說一聲。
>
> **設計上我照最壞情況處理，所以答案是不是猜的其實不影響架構** ——
> 但影響文件的誠實度，`sketchup-api-feasibility.md` 會標成「假設」而非「已驗證」。
>
> 依你的答案（modified? 會變 true、Ctrl+Z 會撤銷到我們的改動），代表擷取會有兩個副作用：
> 1. 使用者關檔時會被問「要儲存嗎」，即使他什麼都沒改。
> 2. 使用者按 Ctrl+Z 想撤銷自己的編輯，卻撤銷到我們的顯示設定。**這比出圖失敗嚴重。**
>
> 處置：擷取全程包在 `ensure` 內確保還原，並在驗證腳本 **V6** 實測能否用
> `start_operation` 的 transparent 參數避開 undo stack。真的避不開，就在 UI 上誠實告知，
> 並列入 README 的「已知限制」。

---

### Q14. 效能

三個 pass（beauty / hidden-line / fog）各輸出 1024×1024，總共花多久？
中間是否需要 `view.refresh` 或等一個 timer tick 才拿得到正確畫面？

**答：1. 你給我一個建議值, 2. 我的判斷是要 **

> **我的回覆**：
>
> **1. 建議值：三個 pass 1024×1024 合計 p50 ≤ 3 s、p95 ≤ 6 s**（已寫在 `spec.md` 4.2）。
> 拆解：每 pass 約 0.6–0.8 s（切設定 + 重繪 + 寫檔），三個約 2 s，留 1 s 給切換與等待。
> 超過 6 s 的處置順序：**先降到 768×768 → 再把 beauty pass 改用 viewport 原生尺寸 →
> 最後才考慮砍 pass**。不要為了省時間犧牲像素對齊，那會毀掉整個論點。
>
> **2. 我同意你的判斷**（需要 `view.refresh`）。這也是我在可行性清單標 🔴 的原因。
> 但要注意一個更麻煩的可能：refresh 之後可能還要**等一個 timer tick** 畫面才真的更新完。
> 若是這樣，三個 pass 就不能寫成同步迴圈，要拆成 `UI.start_timer` 串起來的狀態機。
> 這會改變 `capture/session.rb` 的寫法，所以 V7 要順便量這件事。

---

### Q15. `Sketchup::Http::Request` 能否 PUT 二進位圖檔？

對任一測試端點 PUT 一個 500KB 的 PNG，回報：成功與否、伺服器收到的 byte 數是否相符（有無被當文字破壞）。
若失敗，改試 `require 'net/http'` + HTTPS 是否可用。

**答：OK **

> **我的回覆**：同 Q13，我判斷這是「可以，我會去做」而不是「我測過了，結果是可以」。
>
> 提醒一個容易被誤判的地方：**「有回 200」不等於「檔案完整」。**
> 二進位 body 被當文字處理時，伺服器一樣會回 200，但 PNG 已經壞了。
> 驗證要比對**伺服器收到的 byte 數與 sha256**，不是看狀態碼。
>
> 好消息是退路很穩：bundle 內已確認含 OpenSSL 3.1.0 + Ruby 3.2.2，
> `net/http` 走 HTTPS 幾乎可以確定能用。所以這一項不是 blocker，順序可以排在 fog 標定之後。

---

## C. 背景題

### Q16. 你打算用什麼底模？（SDXL / Flux / SD1.5 / 由 provider 決定）

影響 controlnet 生態的成熟度。多 ControlNet 疊加在 SD1.5/SDXL 上較成熟，Flux 的基礎畫質較好但多控制生態較新。
若你沒有偏好，我建議先用 SDXL + 多 ControlNet 做評估（變因好控制），加分項再試 Flux。

**答：好，目前沒有偏好，可以先選一組先做**

---

### Q17. 評估用的 12 個場景，你有現成的 .skp 嗎？還是要現做/找素材？

若要現做，這會吃掉不少時間，可能需要把場景數降到 6。

**答：沒有現成的 可以先降到 6**

> **我的回覆**：已採納。`spec.md` 的評估規模改為 **6 場景 × 2 相機 = 12 shots**，
> 12 × 3 條件 × 1 seed = 36 張。
>
> 6 個場景建議這樣分配，確保涵蓋會讓結構走鐘的情境（不要 6 個都是方盒子，
> 那樣純 img2img 也不會出錯，測不出差異）：
> 3 外觀（直角量體 / 曲面 / 大面積玻璃）+ 3 內裝（密集開口 / 細長構件如欄杆百葉 / 複雜家具）。

---

### Q18. 【已升級為決策題】要不要把 SketchUp 內建的 su_diffusion 納入評估？

掃描發現 **SketchUp 2026 已內建 Trimble 自家的 AI Render（`su_diffusion`，原生 C++/Qt 擴充）**，
見 critique.md 第 5 點。這改變了競品版圖。

- (a) 把它當成評估第四組 D，和 A/B/C 跑同一批 shots（建議，說服力最高）
- (b) 只做定性比較（截圖 + UI 對照），不進統計
- (c) 不處理

另外：你用過它嗎？出圖品質與速度大概如何？

**答：沒有用過，傾向(c)，等第一版完成當 follow-up issue 處理**

> **我的回覆**：同意，但建議把 (c) 微調成「**(c) + 一段話**」。
>
> 完全不提的風險是：面試官自己知道 SketchUp 2026 內建了這個功能，
> 你的作業卻隻字未提，會被解讀成沒做競品研究。
>
> 花五分鐘在 `critique.md` 補一段「我知道它存在、我看過它的組成（原生 C++/Qt，
> 能取得 Ruby API 拿不到的 depth buffer）、所以我的差異化定位在可控性與可量測性，
> 納入評估列為 follow-up」——成本幾乎為零，但把「疏漏」變成「判斷」。
> 這段我已經寫進 `critique.md` 第 5 點了。

---

### Q19. 這個 repo 要公開還是私有？

目前我建好的是 **private**。若面試方需要看，可以加為 collaborator，或之後一行指令轉公開。

**答：請用 public **

> **⚠️ 這個動作被權限機制擋下，需要你自己執行或授權。**
>
> ```bash
> gh repo edit cbtzeng/sketchup-ai-render-prototype --visibility public --accept-visibility-change-consequences
> ```
>
> 執行前提醒一句（然後就照你的決定做）：面試作業轉公開前，
> 建議確認一下對方沒有「作業內容不得公開」的規定 —— 有些公司會介意題目外流。
> repo 內目前沒有任何金鑰或個資，這方面沒問題。

---

### Q20. 「相關 skills」你想放什麼？

你說要把 skills 放進 repo。我已經建好 `.claude/skills/` 目錄與說明。
請告訴我：是要從別處複製現有的 skills 進來，還是要我幫你針對這個專案寫新的
（例如 `sketchup-ruby-api`、`multi-controlnet-eval`、`ruby-extension-packaging`）？

**答：幫我找電腦中的 DO-system, airexpert_website, duotopia 這幾個專案資料夾中的 .claude/skills/ 找適當地放進來 例如 worktree,fix-review,fix-workflow 等等
也另外幫我新增一些適合的 skills(你提到的這些也可以)**
