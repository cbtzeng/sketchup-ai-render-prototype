# open-questions.md — 待你回覆的問題

**填寫方式**：直接在每題的 `**答：**` 後面寫。留白代表「用我的預設建議」。
你回完之後我才會進下一步（寫 implementation plan）。

分成三類：
- **A. 決策題** —— 只有你能決定，會改變架構或範圍。
- **B. 實機驗證題** —— 需要你在裝了 SketchUp 的機器上跑一下才知道答案。我沒有 SketchUp，猜不得。
- **C. 背景題** —— 影響優先序，不影響架構。

---

## A. 決策題

### Q1. 目標 SketchUp 版本與作業系統？ ✅ 已自動確認
掃描結果：**SketchUp 2026 (26.2.242)、Ruby 3.2.2、macOS 15.7.1、universal binary**。
除非你要同時支援舊版或 Windows，這題不用回答，就鎖 2026 / macOS。

**答：**（若需支援其他版本才填）

---

### Q2. 這份作業的時間盒有多長？（幾天 / 幾小時）
這直接決定 MVP 要不要再砍。
- 若 ≤ 2 天：評估縮到 6 場景 × 2 相機 = 12 shots，只做 Edge F-score 一個指標。
- 若 ≥ 5 天：照 scope.md 的 MVP 全做，並加語意遮罩 pass。

**答：**

---

### Q3. 交付物形式是什麼？（面試方期待看到什麼）
- (a) 可安裝執行的 RBZ + demo 影片
- (b) 程式碼 repo + 設計文件 + 評估報告
- (c) 只有文件與規劃
- (d) 現場口頭簡報 + 投影片

不同答案會讓我把力氣放在完全不同的地方。若是 (b)，評估報告比功能重要；若是 (a)，穩定性比評估重要。

**答：**

---

### Q4. 金鑰模型：平台 key + 額度，還是 BYO key？
見 critique.md 第 6 點——你原本的描述兩者混用了，需要選一個。
- 建議：**平台 key + 固定測試額度**。理由：成本護欄、去重快取、冪等計費在 BYO key 下都失去意義，
  而且保管別人的 API key 是不必要的責任。BYO key 列為文件中的未來選項。

**答：**

---

### Q5. 雲端選 Vercel 還是 Cloudflare Workers？
- 建議：**Vercel**。理由：Node 生態、Supabase SDK 支援直接、webhook 與長一點的 function 執行時間較好處理，
  而這個場景不需要 Workers 的邊緣延遲優勢（瓶頸在 GPU 推論，不在網路 hop）。
- 若你已經有 Cloudflare 的既有基礎建設，就選 Workers，我照改。

**答：**

---

### Q6. fal.ai 和 Replicate，你有帳號與額度嗎？兩家都能測嗎？
architecture.md 第 6 節的比較表目前是**我的預期，不是已驗證事實**。
- 兩家都能測 → MVP 就接一家、加分項補第二家，報告裡放實測 p50/p95 與單張成本。
- 只有一家 → 直接鎖定，比較表改成「文獻/定價頁調查」並註明未實測。

**答：**（有哪家？有無預算上限？）

---

### Q7. 是否採納 critique.md 第 1 點的建議，把「材質/物件語意遮罩」加入論點？
這是我認為最能強化你論點的一項，但會增加範圍（需要遍歷 model 的 material / group 並產生分色圖）。
- (a) 加進 MVP（論點最強，但要多花時間）
- (b) 留在加分項，先在文件中論述（建議，風險低）
- (c) 不做，論點維持 edge + depth

**答：**

---

### Q8. 評估報告若顯示「C 打不贏 B（外部 Canny）」，你要怎麼呈現？
- (a) 誠實寫進報告，並把論點改寫成「護城河在語意與相機真值」（建議，我認為這反而加分）
- (b) 調整實驗設定重跑直到有優勢
- (c) 其他

我需要先知道你的立場，因為這會影響我怎麼寫評估的結論章節。

**答：**

---

## B. 實機驗證題（需要你在 SketchUp Ruby Console 跑）

> 這一節對應 `sketchup-api-feasibility.md` 第 7 節。
> 我沒有 SketchUp 可測，這些我不會猜。答案回填後，🔴 項目才能降級。

### Q9. 環境基本資料 ✅ 大致已確認
已由檔案系統取得（見 Q1）。只剩一件事要你在 Ruby Console 確認：
`RUBY_VERSION` 實際回傳值是否為 `3.2.2`。

**答：**

---

### Q10. `model.rendering_options` 的完整 key 列表與當前值
請 dump 出來整份貼回。之後所有 key 名一律以這份為準——我不會憑印象寫 key。

**答：**
```
（貼在這裡）
```

---

### Q11. write_image 的寬高比行為（最高風險項）
viewport 維持一般比例（例如 16:9），呼叫 `write_image` 輸出 1024×1024，
觀察結果是：被裁切 / 上下加邊 / 重新取景（看到更多東西）？
接著設定 `camera.aspect_ratio` 後重測，行為有無改變？

**答：**

---

### Q12. fog 深度標定（決定成敗）
建一個階梯測試模型：相機正前方 1 / 2 / 5 / 10 / 20 / 50 公尺各放一面白色垂直牆面。
開 fog、fog 色設純黑、關邊線、面設白色，`write_image` 後讀出每面牆中心的灰階值。
請回報：
- (a) 灰階 ↔ 距離的對照表（6 組數字）
- (b) `FogStartDist` / `FogEndDist` 設什麼值
- (c) 背景/天空區域是什麼顏色
- (d) 灰階變化看起來像線性還是指數

**答：**

---

### Q13. 副作用檢查
改 rendering_options 之後：
- `Sketchup.active_model.modified?` 變成 true 嗎？
- 按 Ctrl+Z 會撤銷到我們改的設定嗎？
- 還原後再 dump 一次，跟 Q10 的原始值逐項比對，有無不一致（特別是浮點數）？

**答：**

---

### Q14. 效能
三個 pass（beauty / hidden-line / fog）各輸出 1024×1024，總共花多久？
中間是否需要 `view.refresh` 或等一個 timer tick 才拿得到正確畫面？

**答：**

---

### Q15. `Sketchup::Http::Request` 能否 PUT 二進位圖檔？
對任一測試端點 PUT 一個 500KB 的 PNG，回報：成功與否、伺服器收到的 byte 數是否相符（有無被當文字破壞）。
若失敗，改試 `require 'net/http'` + HTTPS 是否可用。

**答：**

---

## C. 背景題

### Q16. 你打算用什麼底模？（SDXL / Flux / SD1.5 / 由 provider 決定）
影響 controlnet 生態的成熟度。多 ControlNet 疊加在 SD1.5/SDXL 上較成熟，Flux 的基礎畫質較好但多控制生態較新。
若你沒有偏好，我建議先用 SDXL + 多 ControlNet 做評估（變因好控制），加分項再試 Flux。

**答：**

---

### Q17. 評估用的 12 個場景，你有現成的 .skp 嗎？還是要現做/找素材？
若要現做，這會吃掉不少時間，可能需要把場景數降到 6。

**答：**

---

### Q18. 【已升級為決策題】要不要把 SketchUp 內建的 su_diffusion 納入評估？
掃描發現 **SketchUp 2026 已內建 Trimble 自家的 AI Render（`su_diffusion`，原生 C++/Qt 擴充）**，
見 critique.md 第 5 點。這改變了競品版圖。
- (a) 把它當成評估第四組 D，和 A/B/C 跑同一批 shots（建議，說服力最高）
- (b) 只做定性比較（截圖 + UI 對照），不進統計
- (c) 不處理

另外：你用過它嗎？出圖品質與速度大概如何？

**答：**

---

### Q19. 這個 repo 要公開還是私有？
目前我建好的是 **private**。若面試方需要看，可以加為 collaborator，或之後一行指令轉公開。

**答：**

---

### Q20. 「相關 skills」你想放什麼？
你說要把 skills 放進 repo。我已經建好 `.claude/skills/` 目錄與說明。
請告訴我：是要從別處複製現有的 skills 進來，還是要我幫你針對這個專案寫新的
（例如 `sketchup-ruby-api`、`multi-controlnet-eval`、`ruby-extension-packaging`）？

**答：**
